# 2026-07-31

## 清理项目环境（用户要求）
- 删除生成的日志文件：`backend_start.log`、`_fulltest_run.log`、`backend/backend_run.log`（均已被 .gitignore 忽略，属生成物）。
- 删除临时排查脚本 `backend/_check_sys_user.py`（昨日已删）。
- 结束遗留的卡死 uvicorn 进程（PID 19700 及其子进程 31236），释放被占用的 8000 端口。该进程是用户最初 `--reload` 启动但因 `sys_user` 冲突失败的残留 reloader。

## 相关修复（2026-07-30，已落地）
- 登录账号表名 `sys_user` → `da_sys_user`（`backend/app/models/user.py` 的 `__tablename__`，同步更新 `auth.py` 注释）。与业务库 `jb_bi.sys_user` 区分，消除启动期 `Unknown column 'sys_user.password'` 报错。
- 验证：后台启动后端，日志显示走 `da_sys_user` 并成功 seed `admin`，到达 "Application startup complete"。

## 全量测试 + 浏览器端到端验证（2026-07-31）
- 运行 `scripts/run_full_test.py`（后端 8000 + 前端 5173 均启动）：**29/31 通过（93.5%）**。
- 两轮全量测试均只有 2 项失败，且失败项均为**外部依赖/账户配额**问题，非代码缺陷：
  1. **task 对话模式**：首轮因 `harness._build_model` 在 `provider=None`（未指定 model 且无默认 provider 时 `get_llm_config(None)` 返回 None）时直接 `resolve_api_key(None)` 崩 `AttributeError: 'NoneType' object has no attribute 'get'`，中断 SSE 流。
     - **已修复**：`_build_model` 改为复用 `app/core/llm.py` 的 `_resolve_key_and_base(provider)`（对 None 回退 settings），与 legacy 模式一致。重启后端后第二轮该崩溃消失，转为 `403 Free quota exhausted`（LLM 免费额度耗尽，外部）。
  2. **知识库上传 .txt**：`400 文本向量化或 Milvus 入库失败`。实测 Embedding 接口（`embedding-3` @ `https://open.bigmodel.cn/api/paas/v4`，key `740b1b92...`）返回 `429 余额不足或无可用资源包`，属智谱 Embedding 账号额度耗尽，非代码缺陷。Milvus 本身可达（后端与其 ESTABLISHED 连接）。
- 通过的模块：Auth(登录/当前用户/错误密码/缺失字段)、DataSource、Suggestions、Knowledge(列表/删除，仅上传失败)、Export、Schedule、Config、Stop、Approve、Sessions；对话 data/report/doc/多轮 全部通过（LLM 免费额度耗尽时优雅降级为"无法回答此问题"，前端正常渲染 SQL+结果表+导出）。
- **Chrome DevTools MCP 浏览器验证**：admin/12345678 登录成功 → 进入聊天 → 发送"数据库一共有多少张表？" → 流式节点依次渲染（意图分析→生成SQL→执行计划→数据查询→文本回答→质量评估），最终渲染 SQL/结果表/CSV·Excel 导出，**全程无 JS 报错、无中断**。控制台仅 1 条可访问性提示 + 1 条 `[warn] [SSE] 未知事件类型: quality_feedback`（后端发了前端未路由的事件，小瑕疵，不影响流程）。
- 结论：核心功能与对话交互逻辑均正常；2 项失败纯属外部账户额度，需充值后恢复。
