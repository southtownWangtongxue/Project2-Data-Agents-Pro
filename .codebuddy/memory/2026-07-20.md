# 2026-07-20 工作日志

## 任务：清理项目环境 + 全功能测试 + 对话模式验证

### 清理
- 删除 backend 临时探针/日志：`_diag.out`、`_mcp_test.ps1`、`_probe_detail.json`、`_run2.out`、`_run3.out`、`_run_final.out`、`_test_run.out`、`_probe_sse.py`、`_probe_out.txt`。
- 删除 `.codebuddy` 临时调试文件：`test_sse.py`、`debug_response.network-response`、`debug_response_v2.network-response`。
- 删除过期报告 `测试报告_20260713_144539.md`；清理 backend 全部 `__pycache__`。
- 残留：`_fulltest_run.log` 仍被运行中的 run_full_test 进程占用未能删除（用户拒绝了 kill 进程的命令，故保留）。

### 测试结论
- 前后端服务均在线（后端 8000 /health ok；前端 5173 LISTENING）。
- `scripts/run_full_test.py` 阶段1-2（功能模块 API）全部执行完成；阶段3对话测试因 LLM 反复重试耗时过长，多次运行未能写出新报告。
- 单次干净 data 模式 SSE 探针**成功**：66.7s，完整事件流 thread_id→thinking→plan→sql→tool_call→result→token×161→done。
- **关键发现（外部依赖）**：浏览器端到端(Chrome DevTools)登录 admin/12345678 后发送"数据库里一共有多少张表？"，管道逐步执行到"生成 SQL"时报错 **`403 Free quota exhausted (AllocationQuota.FreeTierOnly)`** —— 私有/免费 LLM 免费额度被本轮多次测试耗尽，并非代码缺陷。前端能正确展示错误并提供"重试"按钮。
- 前端控制台仅一条非阻断告警：`[SSE] JSON 解析失败: {"type": "done"}`（对 done 哨兵事件重复解析，可优化但不影响功能）。

### 教训
- 切勿并发运行多个 run_full_test/探针实例：会把免费 LLM 打到限流/额度耗尽，导致 SSE 流开着但长时间无事件（HTTP200 后 90s 无 event）→ 120s 超时 → 3次重试的死循环。测试要串行、单实例。
- 证据截图：`.codebuddy/screenshots/e2e_chat_quota_403.png`。
