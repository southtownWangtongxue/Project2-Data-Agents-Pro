# 2026-07-21 工作记录

## 三项改动（来自上会话）的浏览器端到端验证
启动后端(8000, health 200)与前端(5173)后，用 Chrome DevTools MCP 在 http://localhost:5173 实测：

1. **report 模式 SSE 超时上调（test 脚本）**：`scripts/run_full_test.py` 的 `_sse_chat` 超时 `120s→300s`（`timeout: float = 300, retries: int = 3`）。源码层已确认；属测试脚手架改动，浏览器侧无法直接验证，但应用层无 120s 硬上限（见 MEMORY 坑#4）。
2. **`done` 重复 JSON 解析告警修复**：`frontend/src/composables/useSSE.ts` 过滤尾随控制字符(NUL)并抑制 `done` 哨兵告警。**浏览器验证通过**：task 模式对话在收到 `✓ 完成`(done 事件)后，控制台 warn/error **为空**，告警已消除。
3. **task 模式路由到 DeepAgent**：`backend/app/api/v1/chat.py` 路由 `if use_deep_agent() or body.mode == "task":` → `_stream_chat_deepagent` + `get_deep_agent()`。**浏览器验证通过**：UI 明确显示「DeepAgent 正在分析您的请求...」且模式标记「🤖 通用任务」，确认通用任务已真正走 DeepAgent。

### 遗留（外部依赖，非代码缺陷）
- 实时 LLM 响应仍被免费额度/连接问题阻断：对话返回「处理异常: Connection error. [INTERNAL_ERROR]」并展示「重试」按钮（前端优雅降级正常）。后端进程健康(200)，错误来自 LLM 提供方，非路由/代码回归。

## 调试要点（新增，建议纳入 MEMORY）
- Chrome DevTools MCP 浏览器若报 `browser is already running for ...\chrome-devtools-mcp\chrome-profile`，是上会话遗留 chrome 进程占用 profile 锁。用 PowerShell `Stop-Process` 杀死 CommandLine 含 `chrome-devtools-mcp` 的 chrome.exe 后，MCP 可重新拉起干净浏览器。
- 前端模式切换 `.mode-selector` 菜单由 `mouseenter` 触发（非 click），菜单项 class 为 `.mode-menu-item`；用 `dispatchEvent(new MouseEvent('mouseenter'))` + 点击 `.mode-menu-item` 可脚本化切到「通用任务」。
