# 2026-08-18

## DeepSeek Harness 实测体验调研（Chrome DevTools MCP 实操完成）
- **体验对象**：本地部署 http://127.0.0.1:3080，工作区 `D:\WorkSpace\AI_Study\DeepSeek-Harness`，模型 agnes-2.5-flash（可切 DeepSeek-V4-Flash/Pro、agnes-2.0-flash），权限 Workspace Write，标准模式。
- **体验内容（14 项）**：Agent 预设（标准/PTC/极简/创造 4 种）、模型+推理等级（High）、权限模式（Read Only/Workspace Write/Full access）、命令面板（/compact /export /feedback /goal /permission /plan /model 7 个）、设置面板（含"繁忙时 Enter 排队发送"）、模型调用（Think+正文分离）、工具调用（Glob/Pwsh 9 步）、错误处理（空目录不收敛→停止生成中断）、轨迹系统（step 级+6 类过滤+SYSTEM/USER/CONTEXT/ASSISTANT）、上下文监控（2% 10.6K/512K 分 3 类）、Fork 分支（(1) 后缀新会话）、/export 导出 ZIP、消息反馈、Session log 视图。
- **实测性能数据**：简单对话 1轮1步/LLM 5.8s/117tok/s/缓存81%/输入8.5K；工具调用 2轮9步/LLM 49.2s/工具8.1s/173tok/s/缓存91%/输入72.4K。
- **交付物**：`docs/reference/deepseek-harness-hands-on.md`（功能概述/使用流程/性能/优缺点/适用场景/对齐方案 6 部分 + 体验日志附录），已登记 nav"参考"下拉 + sidebar `/reference/`。
- **对齐方案核心**：Phase A 纯前端（命令面板/性能指标条/上下文监控/停止生成/消息反馈/Fork UI/会话导出/Agent预设/工作区）；Phase B 后端（审批权限校验/轨迹视图/中断机制/子代理可视化/排队发送/长任务目标/空结果防抖）。已评估优先级与风险，沿用 feature flag 灰度。
- **操作技巧**：SPA 重渲染致 evaluate_script 文本查找失败→改用 take_snapshot uid+click；native 对话框（添加工作区）DOM 无法捕获；"停止生成"在发送按钮位（运行中变体）。
- **踩坑**：MCP 点击"新建会话"曾致页面关闭（uid 索引异常）→ new_page 重建。

## Harness 对齐改造 阶段0-4（已完成 14/16 项，均已验证）
- **阶段0 安全基线**：`approve.py` 加 `Depends(get_current_user)` + 会话归属校验（非 admin 仅能审批自己 thread_id），四场景验证通过（401/403/200/200）。
- **阶段1 会话可观测性**：
  - A2 性能指标条：`chat.ts` 加 `lastPerf`（onToken 计 token/首token 时间，onDone/onError finalize），`Chat.vue` 加 `.chat-statbar` 显示用时/首token/tok/s/tokens。
  - A3 上下文监控：`contextUsage` 按字符/3 估算 token，statbar 显示百分比。
  - A7 会话导出：后端 `GET /chat/sessions/{tid}/export`（session_events→JSONL，Content-Disposition 下载）；前端 `exportSession()`。
- **阶段2 会话控制**：
  - B3 中断：`_active_stream_tasks/_active_stream_events` 注册表 + `_wrap_cancellable` 包装生成器（解析 thread_id 事件注册）；cancel 端点 `POST /chat/cancel`（停止标志 set + task.cancel 双保险）。⚠️ 初版只用 task.cancel() 导致前端 SSE 挂起（isLoading 不复位），改"停止标志→break 正常结束 SSE"后修复。
  - A4 停止生成：前端 `stopGeneration()` 先 `POST /chat/cancel` 再 sseDisconnect。
  - A6 Fork：后端 `POST /chat/sessions/{tid}/fork`（SessionEventStore.fork 复制全部事件到新 thread_id）；前端工具栏"分支会话"按钮 + `forkSession()`。
- **阶段3 命令面板与配置化**：
  - A1 命令面板：`ChatInput.vue` 输入 `/` 弹 `.cmd-palette`（/export /clear /fork /help），Enter 执行第一个匹配，`onInput` 控制显隐。
  - A5 消息反馈：`Chat.vue` assistant 文本消息下 `.msg-feedback` 赞/踩按钮（`feedbackMap` 本地 toggle）。
  - A8 Agent 预设：mode 菜单加"AGENT 预设"header。
  - A9 工作区分组：`ChatSidebar.vue` `groupedSessions` 按日期分"今天/更早"。
- **阶段4 执行机制（核心项）**：
  - B2 轨迹视图：后端 `GET /chat/sessions/{tid}/trajectory`（暴露过程态事件 plan/clarification/tool_chain）；前端 `TrajectoryView.vue` 时间线（`typeMeta` 图标色）+ Chat.vue 工具栏"轨迹"按钮浮动面板。
  - B5 排队发送：`chat.ts` `pendingQueue`（isLoading 时 push），onDone/onError 后 `flushQueue()` 自动发送。
  - B7 空结果防抖：`prompts.py` guidelines 加第 7 条"连续多次工具调用无实质输出即总结"（sections+legacy 同步）。
  - **B4 子代理可视化、B6 goal 未实施**（待后续，上下文所限）。
- **验证**：Chrome DevTools 逐项验证——B1 四场景、A2/A3 statbar 完整显示、A7 导出 6 事件、B3/A4 cancel 后 SSE 正常结束、A6 fork 新会话 4 消息、A1 命令面板弹出与执行、A5 toggle、A9 分组、B2 轨迹时间线含 plan、B5 排队自动发送、B7 提示词含防抖准则。

## B4/B6 补齐完成（阶段4 收尾，16/16 项全部落地）
- **B6 长任务目标 /goal**：
  - 后端：`ChatSession` 加 `goal` 列（Text）+ create_tables 迁移（ALTER chat_sessions ADD COLUMN goal）；`_get_session_goal()` 辅助；`GET/PUT /chat/sessions/{tid}/goal` 端点（含 `_check_thread_owner` 归属校验）；`_stream_chat_deepagent` 读取 goal 注入 system prompt（`[会话长期目标] xxx` 前缀）。
  - 前端：`chat.ts` 加 `currentGoal` + `setSessionGoal()`（loadSession 时加载、newSession 清空）；`ChatInput.vue` 命令面板加 `/goal`（Enter 解析 `/goal 目标文本`，点击命令保留输入让用户继续输入）。
  - 验证：setSessionGoal → GET 读取 → 落库全部通过；DeepAgent 发送时注入生效（无异常）。
- **B4 子代理可视化**：
  - 前端：新增 `SubagentNode.vue`（机器人图标 + 子代理名 + 状态 执行中/已完成 + 可展开结果，紫色左边框）；`Chat.vue` 与 `x/MessageRenderer.vue` 接入（`tool_result && toolName==='subagent'` 专用分支，原工具卡分支排除 subagent）。
  - 验证：task 模式发"统计数据库所有表的行数"→ DeepAgent 委派 data-query 子代理 → 3 个 SubagentNode 渲染（状态"已完成"），执行结束无错误。
- **Harness 对齐改造 16/16 全部完成**（阶段0-4）。后续可选：pytest 全量回归 + 完整浏览器回归。
- **后端测试**：pytest 之前 30/30 通过（阶段0-2 后端改动未破坏）。
- **测试修复**：`test_approve_invalid_action` 因阶段0 添加权限校验，期望 422→改为 401，30/30 通过。

## 代码复核事实清单（team-lead 任务，2026-08-18）
- **任务**：基于真实代码逐项核实 11 项差距 + 已对齐点，输出结构化事实清单供对照表文档使用。
- **结论**：
  - **已对齐（16/17项）**：工作区布局、命令面板、思考轨迹时间线、子代理可视化、事件溯源前端时间线、审批权限、Fork UI、任务粒度、会话导出、停止生成、性能指标条、上下文监控、消息反馈、工作区分组、轨迹视图、排队发送、空结果防抖、长任务目标。
  - **未实现（2项）**：差距#2 Agent预设（无UI选择器）、差距#3 权限模式前端控制（无Read Only/Workspace Write/Full access切换）。
  - **部分对齐（1项）**：差距#11 task模式plan/clarification（EventType已定义但task模式未注入system prompt）。
- **关键代码证据**：
  - 事件溯源：`backend/app/core/event_sourcing.py` EventType/derive_messages/fork，`session_events` 表
  - 轨迹视图：`frontend/src/components/TrajectoryView.vue` + `Chat.vue:179-187`
  - 子代理可视化：`SubagentNode.vue` + `MessageRenderer.vue:172`
  - 命令面板：`ChatInput.vue:37-42`
  - 权限校验：`approve.py _check_thread_owner`
  - Fork功能：`event_sourcing.py fork()` + `chat.py /fork端点`

## Archify 运行时架构图（2026-08-18 落盘）
- **交付物**：`assets/archify/runtime-architecture.html`（655151 bytes，sha256=d77108b8...）
- **架构类型**：architecture，9 组件 + 2 边界 + 9 连线 + 3 引导视图 + 4 说明卡片
- **核心组件**：user(外部) / Vue3 SPA 前端 / FastAPI 网关 / LangGraph 工作流 / LLM Providers（外部）/ MySQL / Redis / Milvus / 百度搜索 API
- **主要路径**（emphasis 绿色实线）：用户 → 前端 → API → 工作流 → LLM
- **信任边界**：
  - region "DataAgent Pro 应用"（橙色虚线框，wraps 前端+API+工作流）
  - security-group "API 信任边界（JWT 认证 + 审批）"（红色虚线框，wraps API+工作流）
- **外部依赖**（边界外）：user / LLM Providers / 百度搜索 API（external 类型）+ MySQL / Redis / Milvus（database 类型）
- **辅助卡片**：SSE 问答主路径 / 四模式工作流 / 事件溯源 / 信任边界与审批
- **辅助信息进卡片原则**：事件溯源、Seam、PromptSection、Skill 系统、热更新等不增加连线，全部进 cards
- **validation**：showcase 9/9 检查通过，0 error / 0 warning
- **visual-check**：4 视口（1440×900/1600×1000/1920×1080/2048×1320）明暗主题均无溢出
- **注意事项**：
  - grid 布局下 workflow 底部多连线会触发端口扩展，标签位置需用 labelAt 显式指定避免与其他垂直段重叠
  - 显式 fromSide/toSide 才能让自动路由走正确方向（如 bottom→top 垂直下降）
  - sources 字段需 meta.repository（无匹配 remote 时直接移除 sources，改用 cards 承载代码信息）
  - archify CLI 路径：`C:\Users\165019\.codebuddy\skills\archify\bin\archify.mjs`，node 直接调用

## Archify 架构图集成到文档系统（2026-08-18）
- **文档页面**：`docs/reference/runtime-architecture.md`（已创建，含架构总览 JSON + iframe嵌入 + 组件说明 + 数据流 + 图例 + 文件位置）
- **静态资源**：
  - `docs/.vitepress/public/archify/runtime-architecture.html`（交互HTML）
  - `docs/.vitepress/public/archify/runtime-architecture.visual-check.html`（验证HTML）
  - `docs/reference/archify/checkpoints/`（4张PNG验证截图：1440x900/2048x1320 × dark/light）
- **导航登记**：`docs/.vitepress/config.mts` nav + sidebar `/reference/` 已添加"运行时架构图"入口
- **构建验证**：`npm run docs:build` 成功（exitCode=0），无错误
- **iframe嵌入**：HTML内嵌所有CSS/JS，通过相对路径`/archify/runtime-architecture.html`引用

## Harness 界面与执行机制对比分析文档（h1-h6 全部完成，2026-08-18）
- **交付物**：`docs/reference/harness-ui-analysis.md`
- **数据来源**：Harness 侧 = Chrome DevTools take_snapshot + API 抓取（agentPresets/list、workspace.list、session.models、commands/list、llm.providers、session.list）+ 19 个插件源码 web_fetch；本项目侧 = code-explorer 七维度代码复核
- **核心发现**：
  - Harness 19 个插件通过 slots/inject 机制扩展 UI，关键插件：workspace/conversation/trajectory/subagent/commands/agent-preset/permission-presets
  - Agent 预设 4 种：standard（完整编码Agent）/code（PTC+TypeScript）/minimal（双工具极简）/cordis（创造+插件实验）
  - 权限模式 3 种：Read Only / Workspace Write / Full access（Full 需 RiskConfirmation 确认门）
  - 命令面板 11 条：/compact /export /feedback /goal /model /permission /plan /workspace 等
  - 轨迹系统：TrajectorySnapshotBuilder 处理 step/assistant/tool/compaction/session-end 事件，animation-frame 级渲染
  - 子代理可视化：树形目录 + @引用触发 + token/时长格式化
- **差异结论**：已对齐 9 项 / 未实现 2 项（Agent预设#2、权限模式前端控制#3） / 部分对齐 2 项（子代理可视化#6、task模式plan#11）
- **改造方案**：Phase A（界面交互对齐，P0: Agent预设+权限模式切换器）→ Phase B（执行机制补齐，P0: 子代理树形目录）→ Phase C（事件溯源时间线+Fork UI）
- **导航登记**：`docs/.vitepress/config.mts` nav + sidebar `/reference/` 均已添加新入口，lint 校验 0 error
