# 2026-06-30 工作记录

## 前端体验优化（15:10）

### 1. 自动滚动到底部
- 新增 `smartScrollToBottom()`：仅在用户未手动上滚（距底部 < 120px）时追底
- `lastMsgContent` computed 追踪最后一条消息内容变化
- `loadSession` 后调用 `scrollToBottomDelayed(200)` 确保 DOM 渲染后滚动
- watch `[messages.length, lastMsgContent]` 触发智能滚动

### 2. 任务清单改为浮动面板
- 从文档流中移除，改为 `position: absolute; top/right` 悬浮在消息区右上角
- 运行时：金色光晕 + 呼吸点动画 → "执行中" chip
- 完成后：绿色淡雅 → "✓ X/Y 完成" chip
- 点击展开下拉面板，不遮挡消息内容
- `.chat-main` 添加 `position: relative`

### 3. 首页文档链接使用动态地址
- `DOCS_BASE = \`http://${window.location.hostname}:5174\``
- 支持 localhost 和 LAN IP 自动适配
